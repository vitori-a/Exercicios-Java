import java.util.Scanner;

class Main {
  public static void main(String[] args) {
    float salario;
    Scanner ler = new Scanner(System.in);

    System.out.println("Insira o valor do salário que deve ser reajustado: ");
    salario = ler.nextFloat();

    if(salario <= 2000) {
      salario = salario*(float)1.5;
      System.out.print("\nO valor á ser reajustado é de "+salario);
    }
    else { if (salario <= 5000) {
      salario = salario*(float)1.2; 
      System.out.print("\nO valor á ser reajustado é de "+salario);
    }
    else
      salario = salario*(float)1.1;
      System.out.print("\nO valor á ser reajustado é de "+salario);
    } 
  }
}
